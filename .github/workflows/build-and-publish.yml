name: Java Build and Publish

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3  # Updated to use Node.js 20

      - name: Set up JDK
        uses: actions/setup-java@v2  # Ensure this version supports Node.js 20
        with:
          java-version: '11'

      - name: Build with Gradle
        run: ./gradlew build

      - name: Run tests
        run: ./gradlew test

      - name: Publish artifacts
        run: |
          ./gradlew publish

    # Add more steps for release and publishing to package registry if needed
